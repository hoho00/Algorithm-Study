#include<bits/stdc++.h>
using namespace std;
int main(){	
	vector<int> a={1, 3, 2, 3, 2, 3, 2, 3, 1, 2};
	set<int> s;
	for(int i=0; i<a.size(); i++){
		s.insert(a[i]);
	}
	cout<<s.size()<<endl;
	/*for(auto it=s.begin(); it!=s.end(); it++){
		cout<<*it<<" ";
	}*/
	for(auto x : s) cout<<x<<" ";
	cout<<endl;
	if(s.find(7)==s.end()) cout<<"false";
	else cout<<"true";
	return 0;
}
