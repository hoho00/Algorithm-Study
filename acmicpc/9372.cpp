#include <iostream>

using namespace std;

int main()
{
	int T;
	cin >> T;
	
	for (int k = 0; k < T; k++) {
		int N, M;
		cin >> N >> M;
		cout << N - 1 << endl;
		for (int i = 0; i < M; i++) {
			int tmp1, tmp2;
			cin >> tmp1 >> tmp2;
		}
	}
	return 0;
}
